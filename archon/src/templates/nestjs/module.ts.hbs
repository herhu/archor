import { Module } from '@nestjs/common';
import { TypeOrmModule } from '@nestjs/typeorm';
{{#each services}}
import { {{className}} } from '{{importPath}}';
{{/each}}
{{#each controllers}}
import { {{className}} } from '{{importPath}}';
{{/each}}
{{#each entities}}
import { {{this}} } from './entities/{{this}}.entity';
{{/each}}

@Module({
imports: [
TypeOrmModule.forFeature([
{{#each entities}}
{{this}},
{{/each}}
]),
],
controllers: [
{{#each controllers}}
{{className}},
{{/each}}
],
providers: [
{{#each services}}
{{className}},
{{/each}}
],
})
export class {{moduleClassName}} {}
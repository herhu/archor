export class Create{{entity.name}}Dto {
{{#each entity.fields}}
{{#unless primary}}
{{name}}: {{#if (eq type 'boolean')}}boolean{{else}}{{#if (eq type 'int')}}number{{else}}string{{/if}}{{/if}};
{{/unless}}
{{/each}}
}
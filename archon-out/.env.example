# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/validation app

# JWT verification
JWT_ISSUER=https://auth.example.com
JWT_AUDIENCE=https://api.example.com

# Choose ONE verification mode:

# (A) RS256 / OIDC providers (recommended)
JWT_JWKS_URI=https://auth.example.com/.well-known/jwks.json

# (B) HS256 shared secret (simple internal use)
# JWT_SECRET=supersecret

# Port
PORT=3000